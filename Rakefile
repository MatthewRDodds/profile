# # %w{ bundler find rake/testtask}.each { |lib| require lib }
# require 'bundler'

# task :default => :compile_assets

# task :compile_assets do |t|
# 	APP_FILE  = 'app.rb'
# 	APP_CLASS = 'Sinatra::Application'

# 	require 'sinatra/assetpack/rake'
# end